{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Flask app (external terminal)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/app.py",
      "console": "externalTerminal",
      "cwd": "${workspaceFolder}",
      "env": {
        "FLASK_ENV": "development"
      }
    }
  ]
}